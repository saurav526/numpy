🔹 Logical & Comparison Functions (One-Line Each)

greater() – Compares two arrays and returns True where the first is greater.

less() – Compares two arrays and returns True where the first is smaller.

equal() – Checks whether corresponding elements of two arrays are equal.

not_equal() – Checks whether corresponding elements of two arrays are not equal.

logical_and() – Returns True when both conditions are True.

logical_or() – Returns True when at least one condition is True.

logical_not() – Reverses the Boolean values of a condition.

any() – Returns True if at least one element satisfies the condition.

all() – Returns True if all elements satisfy the condition.

where() – Selects elements based on a given condition.